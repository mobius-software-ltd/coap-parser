package com.mobius.software.coap.parser.message.options;

public class CoapOptionEmpty extends CoapOptionValue
{
	public CoapOptionEmpty(int type, int length, byte[] value)
	{
		super(type, length, value);
	}
}
